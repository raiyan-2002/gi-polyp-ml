# gi-polyp-ml

dataset can be found [here](https://datasets.simula.no/kvasir-seg/) (kvasir-seg.zip)

this assumes that the data is at /data of the project root in this format:

```
/data
    /images
    /masks
    boxes.json
```